import React from "react";
import footerStyles from "./footer.module.scss";

const Footer = () => {
  return (
      <footer className={footerStyles.block}>
          <div>footer</div>
    
    </footer>
  );
};

export default Footer;